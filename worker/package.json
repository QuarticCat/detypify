{
    "name": "detypify-worker",
    "version": "0.2.4",
    "private": true,
    "scripts": {
        "dev": "wrangler dev",
        "deploy": "wrangler deploy"
    },
    "dependencies": {
        "hono": "^4.2.2"
    },
    "devDependencies": {
        "wrangler": "^3.48.0"
    }
}
